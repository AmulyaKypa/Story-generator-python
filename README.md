this code is a python code that generates random story with the help of gemini api
the api is from free tier
this code takes genre, no. of paras and 3-5 keywords as input and generates a story within 5 attempts
if it is not able to generate then that output is printed
